# Agent API Path

This guide is now part of the [Agent quickstart](AGENT_QUICKSTART.md#http-api-path):
the `/api/v1/*` route sequence, `plan` statuses and detail levels, example calls, and policy
context.
