# Agent API Path

This guide is now part of the [Agent quickstart](AGENT_QUICKSTART.md#http-api-path):
the `/api/v1/*` route sequence, `plan` statuses and detail levels, example calls, and policy
context. The headings below keep old links working.

## Recommended Loop

Moved to [Agent quickstart: Recommended Loop](AGENT_QUICKSTART.md#recommended-loop).

## Plan Status And Detail

Moved to [Agent quickstart: Plan Status And Detail](AGENT_QUICKSTART.md#plan-status-and-detail).

## Minimal Agent Calls

Moved to [Agent quickstart: Minimal Agent Calls](AGENT_QUICKSTART.md#minimal-agent-calls).

## Policy Context

Moved to [Agent quickstart: Policy Context](AGENT_QUICKSTART.md#policy-context).
