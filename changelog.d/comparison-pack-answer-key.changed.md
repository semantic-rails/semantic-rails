- The semantic-layer comparison pack checks every layer, Semantic Rails included, against an
  independent answer key: SQL written against the shared views without seeing any layer's
  models or outputs, reviewed by a second author. Each layer's result columns are mapped to the
  answer's fields explicitly instead of being guessed from their names.
