- The Architect MCP states its workflow and write contract once, in the server
  instructions. Every tool now has a title, behavior hints and a shorter
  description, and input schemas drop generated titles: the tool list an agent
  reads is 16% smaller. `project_status` reports the package's warehouse,
  dialect and connection kind, and flags a connection kind the warehouse
  doesn't accept.
