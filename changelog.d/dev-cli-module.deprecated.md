- `semantic_rails.dev_cli` is deprecated. The human CLI now lives in
  `semantic_rails.cli` (commands, reports and output) and `semantic_rails.repl`;
  `semantic_rails.dev_cli` re-exports its public names until the next minor release.
