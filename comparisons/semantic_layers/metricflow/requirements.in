# The MetricFlow comparison harness. Regenerate requirements.lock after editing:
#   uv pip compile --python-version 3.12 --prerelease=if-necessary-or-explicit \
#     --generate-hashes requirements.in -o requirements.lock
dbt-metricflow==0.15.0
dbt-duckdb==1.11.0
