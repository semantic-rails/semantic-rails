model:
  id: stores
  relation: comparison_stores
  entities:
    store: {}
  dimensions:
    store_name:
      # Preserve the public ID used by comparison query JSONs.
      as: dimension.jaffle_store_name
      label: Store name
      kind: categorical
